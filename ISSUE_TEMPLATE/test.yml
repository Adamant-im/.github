name: "🧪 Test form"
description: "Minimal issue form to check YAML support"
title: "[Test] "
body:
  - type: input
    id: summary
    attributes:
      label: "Summary"
      description: "Short description of the problem"
    validations:
      required: true